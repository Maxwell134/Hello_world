# .github/workflows/use-test-action.yml
name: Use Test Action

on:
  push:
    branches:
      - main

jobs:
  use-test-action:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Use Test Action
        uses: Maxwell134/test321@main
        with:
            name: 'tommy'
        
       
